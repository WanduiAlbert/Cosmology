[user]
# CUSTOMIZE(name)
  name = Albert Wandui
  email = albertkamau16@gmail.com
[push]
	default = simple
[color]
  # colorize by default
	ui = true

# vim: ft=gitconfig
[filter "media"]
	clean = git media clean %f
	smudge = git media smudge %f
	required = true
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
